/**
 *  Do not remove this unless you get business authorization.
 *  Category
 *  init by [stategen.progen] ,can be edit manually ,keep when "keep this"
 *  由 [stategen.progen]代码生成器初始化，可以手工修改,但如果遇到 keep this ,请保留导出设置以备外部自动化调用
 */
import {Effect, Effects, Reducers, Subscriptions} from '@utils/DvaUtil';
import Category from "@i/beans/Category";
import {CategoryState} from '@i/interfaces/CategoryFaces';

/*** keep this export */
export const categoryCustomState = {

}

/*** keep this export */
export interface CategoryCustomSubscriptions extends Subscriptions {

}

/*** keep this export */
export interface CategoryCustomEffects extends Effects {

}

/*** keep this export */
export interface CategoryCustomReducers extends Reducers<CategoryState> {

}
