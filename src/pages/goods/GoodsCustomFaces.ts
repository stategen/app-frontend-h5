/**
 *  Do not remove this unless you get business authorization.
 *  Goods
 *  init by [stategen.progen] ,can be edit manually ,keep when "keep this"
 *  由 [stategen.progen]代码生成器初始化，可以手工修改,但如果遇到 keep this ,请保留导出设置以备外部自动化调用
 */
import {Effect, Effects, Reducers, Subscriptions} from '@utils/DvaUtil';
import Goods from "@i/beans/Goods";
import PageList from "@i/beans/PageList";
import {GoodsState} from '@i/interfaces/GoodsFaces';

/*** keep this export */
export const goodsCustomState = {

}

/*** keep this export */
export interface GoodsCustomSubscriptions extends Subscriptions {

}

/*** keep this export */
export interface GoodsCustomEffects extends Effects {

}

/*** keep this export */
export interface GoodsCustomReducers extends Reducers<GoodsState> {

}
