/**
 *  Do not remove this unless you get business authorization.
 *  GoodsDetail
 *  init by [stategen.progen] ,can be edit manually ,keep when "keep this"
 *  由 [stategen.progen]代码生成器初始化，可以手工修改,但如果遇到 keep this ,请保留导出设置以备外部自动化调用
 */
import {Effect, Effects, Reducers, Subscriptions} from '@utils/DvaUtil';
import Goods from "@i/beans/Goods";
import {GoodsDetailState} from '@i/interfaces/GoodsDetailFaces';

/*** keep this export */
export const goodsDetailCustomState = {

}

/*** keep this export */
export interface GoodsDetailCustomSubscriptions extends Subscriptions {

}

/*** keep this export */
export interface GoodsDetailCustomEffects extends Effects {

}

/*** keep this export */
export interface GoodsDetailCustomReducers extends Reducers<GoodsDetailState> {

}
